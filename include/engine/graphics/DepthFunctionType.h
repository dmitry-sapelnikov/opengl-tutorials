#pragma once

// Includes

namespace gltut
{

/// Depth function types
enum class DepthFunctionType
{
	NEVER,
	LESS,
	EQUAL,
	LEQUAL,
	GREATER,
	NOTEQUAL,
	GEQUAL,
	ALWAYS
};

// End of the namespace gltut
}
